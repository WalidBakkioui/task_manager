{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur Task-Manager{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('homepage') }}">Task-Manager</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <div class="row align-items-center">
            <div class="col-md-6 text-md-start">
                <h1 class="display-4 fw-bold text-primary mb-4">Gérez vos tâches avec simplicité</h1>
                <p class="fs-5 text-muted">
                    Task-Manager est votre allié pour organiser vos journées avec efficacité. Planifiez, priorisez et suivez vos tâches en quelques clics grâce à une interface moderne et intuitive.
                </p>
                <p class="text-muted">
                    Que vous soyez étudiant, professionnel ou freelance, notre outil s’adapte à vos besoins pour vous faire gagner du temps et rester concentré sur l’essentiel.
                </p>
                <p class="text-muted">
                    Toutes vos tâches sont accessibles en ligne, depuis n’importe quel appareil. Ne laissez plus rien vous échapper.
                </p>
                <div class="mt-4">
                    <a href="{{ path('login') }}" class="btn btn-primary btn-lg me-2">Commencer maintenant</a>
                </div>
            </div>
            <div class="col-md-6 text-center">
                <img src="{{ asset('assets/images/homepage.png') }}" alt="Illustration gestion de tâches" class="img-fluid rounded shadow">
            </div>
        </div>
    </div>
{% endblock %}