{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur Task-Manager{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('homepage') }}">Task-Manager</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container text-center mt-5">
        <img src="{{ asset('assets/images/logo.png') }}" alt="Logo Task-Manager" class="img-fluid mb-4" style="max-width: 220px;">
        <h1 class="display-5 fw-bold">Bienvenue sur Task-Manager</h1>
        <p class="lead text-muted mt-3">
            Task-Manager est une application web simple et intuitive qui vous permet de créer, organiser et suivre vos tâches au quotidien.
            Grâce à une interface moderne, vous pouvez gérer vos priorités, consulter l'historique de vos tâches terminées, et accéder à votre profil personnalisé.
        </p>
        <p class="text-muted">
            Connectez-vous pour commencer à gérer vos tâches, ou créez un compte en quelques secondes.
        </p>
        <div class="mt-4">
            <a href="{{ path('login') }}" class="btn btn-primary btn-lg me-2">Connexion</a>
            <a href="{{ path('app_register') }}" class="btn btn-success btn-lg">Inscription</a>
        </div>
    </div>
{% endblock %}
